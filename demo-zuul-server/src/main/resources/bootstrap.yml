server:
  port: 8080
spring:
  application:
    name:zuul-server
  cloud:
    config:
      uri: http://config-server:8888
  profiles:
    active: vdi
eureka:
  client:
    enabled: true
    server-url:
      defaultZone: http://eureka-server:8761/eureka/
